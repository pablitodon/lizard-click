# Определяем стандартные команды для работы с Docker-контейнером

# Сборка Docker-образа
# Команда сборки образа:
# - docker build - собирает образ
# - -t tgbot    - задает имя образа (tag)
# - .           - указывает на Dockerfile в текущей директории
build:
	docker build -t tgbot .

# Запуск контейнера
# Команда запуска контейнера:
# - docker run   - запускает контейнер
# - -d          - запуск в фоновом режиме (detached)
# - -p 3000:3000 - пробрасывает порт (хост:контейнер)
# - --name tgbot - задает имя контейнеру
# - --rm        - автоматически удаляет контейнер после остановки
# - tgbot       - имя образа для запуска
run:
	docker run -d -p 3000:3000 --name tgbot --rm tgbot